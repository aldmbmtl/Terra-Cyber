apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-squid
spec:
  selector:
    kuiper.juno-innovations.com/kuiper-instance: "{{ .Values.name }}"
  ports:
    - port: 3128
      targetPort: 3128
      protocol: TCP
      name: http
